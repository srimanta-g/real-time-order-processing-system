spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/srimanta-g/real-time-order-processing-config'

eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/
    instance:
        prefer-ip-address: true

server:
  port: 8888
